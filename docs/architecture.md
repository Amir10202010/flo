# Architecture

Monolith Next.js app (App Router) with server route handlers.

Core services: GeminiService, TelegramSync, GmailSync, PriorityEngine, ConversationAnalyzer.

Database: Supabase Postgres via Prisma ORM.

Deployment: Vercel.
